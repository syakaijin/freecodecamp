function sumAll(arr) {
  
  var min = Math.min.apply(this,arr);
  var max = Math.max.apply(this,arr);
  var sum = 0;
  
  for(i=min; i<=max; i++) {
    sum += i;
  }
  
  return sum;
}

sumAll([1, 4]);
